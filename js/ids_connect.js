/*globals jQuery, Drupal, SwaggerApi, ApiKeyAuthorization*/
(function ( window, $, undefined ) {
  'use strict';
  var log = Function.prototype.bind.call(console.log, console);

  Drupal.behaviors.ids_connect = {
    attach: function( context, settings ) {
      
    }
  };
});
